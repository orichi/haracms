%h1 Listing messages

%table
  %tr
    %th Title
    %th Content
    %th
    %th
    %th

  - @messages.each do |message|
    %tr
      %td= message.title
      %td= message.content
      %td= link_to 'Show', admin_message_path(message)
      %td= link_to 'Edit', edit_admin_message_path(message)
      %td= link_to 'Destroy', admin_message_path(message), :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Message', new_admin_message_path
